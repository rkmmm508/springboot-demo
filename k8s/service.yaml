apiVersion: v1
kind: Service
metadata:
  name: person-service
spec:
  selector:
    app: person
  ports:
    - protocol: TCP
      port: 8081           # service port
      targetPort: 8081     # container port
  type: NodePort
